{
  "name": "lakeburner",
  "displayName": "LakeBurner",
  "description": "User-Approval Harness for AI coding assistants. Sits beside Copilot Chat, Claude Code, and Codex inside VS Code, monitors their presence, and gates risky actions through @lakeburner.",
  "version": "0.8.6",
  "publisher": "skooma-labs",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/skooma-labs/LakeBurner"
  },
  "bugs": {
    "url": "https://github.com/skooma-labs/LakeBurner/issues"
  },
  "homepage": "https://github.com/skooma-labs/LakeBurner#readme",
  "keywords": [
    "vscode",
    "extension",
    "copilot",
    "claude",
    "codex",
    "chat",
    "ai",
    "approval",
    "harness",
    "auto-run"
  ],
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "engines": {
    "vscode": "^1.119.0"
  },
  "icon": "resources/flame-icon.png",
  "main": "./dist/main.js",
  "activationEvents": [
    "onChatParticipant:lakeburner.harness",
    "onLanguageModelTool:lakeburner_decide"
  ],
  "contributes": {
    "commands": [
      {
        "command": "lakeburner.autoRun.toggle",
        "title": "LakeBurner: Toggle Auto-Run",
        "category": "LakeBurner"
      },
      {
        "command": "lakeburner.activity.popout",
        "title": "LakeBurner: Open Activity Popout",
        "category": "LakeBurner"
      },
      {
        "command": "lakeburner.sendInitialPrompt",
        "title": "LakeBurner: Start a Chat",
        "category": "LakeBurner"
      },
      {
        "command": "lakeburner.manageAccounts",
        "title": "LakeBurner: Manage Overlord Accounts",
        "category": "LakeBurner"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "lakeburner-container",
          "title": "LakeBurner",
          "icon": "resources/flame-icon.svg"
        }
      ]
    },
    "views": {
      "lakeburner-container": [
        {
          "id": "lakeburner-view",
          "name": "",
          "icon": "resources/flame-icon.svg",
          "type": "webview"
        }
      ]
    },
    "chatParticipants": [
      {
        "id": "lakeburner.harness",
        "name": "lakeburner",
        "fullName": "LakeBurner",
        "description": "User-Approval Harness for AI coding assistants.",
        "isSticky": true,
        "commands": [
          {
            "name": "approve",
            "description": "Request user approval for a proposed action. Defaults to the safest direction on cancel."
          },
          {
            "name": "context",
            "description": "Return local workspace context that an AI assistant can use to ground its next step."
          },
          {
            "name": "advise",
            "description": "Recommend the safest direction for a proposed plan."
          },
          {
            "name": "start",
            "description": "Start Auto-Run for this chat and add it to Active Fires."
          },
          {
            "name": "stop",
            "description": "Stop Auto-Run and clear Active Fires."
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "lakeburner_decide",
        "displayName": "LakeBurner Decide",
        "modelDescription": "Ask the user (via the LakeBurner harness) for approval of an action or for direction on which approach to take. When the user has Auto-Run enabled, returns instant approval or the trust phrase 'Keep going, I trust your intuitions'. Use this whenever you would otherwise need to pause and ask the user a yes/no question or which-direction question.",
        "userDescription": "Routes Copilot's approval and direction questions through LakeBurner.",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "lakeburner",
        "icon": "resources/flame-icon.svg",
        "tags": [
          "approval",
          "harness",
          "lakeburner"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "approve",
                "decide"
              ],
              "description": "'approve' for yes/no approval requests; 'decide' when asking which direction to take."
            },
            "question": {
              "type": "string",
              "description": "The question or proposed action being approved."
            }
          },
          "required": [
            "question"
          ]
        }
      }
    ],
    "configuration": [
      {
        "title": "General",
        "properties": {
          "lakeburner.debugLevel": {
            "type": "string",
            "default": "Quiet",
            "enum": [
              "Silent",
              "Quiet",
              "Loud"
            ],
            "enumDescriptions": [
              "No output whatsoever.",
              "Step progress only (TASK events).",
              "Full verbose diagnostics. Steps and detailed information."
            ],
            "description": "The level of Verbosity Expected from LakeBurner in your Console Output.",
            "scope": "application"
          },
          "lakeburner.localCommandInjection.enabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "When `true`, LakeBurner listens on a local TCP port (`127.0.0.1:19816`) for command injection from trusted local applications (e.g. IcedPlate Hermaeus). Only accepts connections from localhost.",
            "scope": "application"
          },
          "lakeburner.localCommandInjection.port": {
            "type": "number",
            "default": 19816,
            "minimum": 1024,
            "maximum": 65535,
            "markdownDescription": "TCP port for the local command injection listener. Default `19816`. Only used when `lakeburner.localCommandInjection.enabled` is `true`.",
            "scope": "application"
          }
        }
      },
      {
        "title": "Overlords",
        "properties": {
          "lakeburner.providers": {
            "type": "array",
            "default": [
              {
                "id": "GitHub.copilot-chat",
                "label": "GitHub Copilot"
              },
              {
                "id": "anthropic.claude-code",
                "label": "Claude Code"
              },
              {
                "id": "openai.chatgpt",
                "label": "OpenAI Codex"
              }
            ],
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "object",
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "label": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "id"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "markdownDescription": "Provider extensions shown in the LakeBurner **Overlords** status panel. Stored account credentials are managed with the command [`LakeBurner: Manage Overlord Accounts`](command:lakeburner.manageAccounts).",
            "scope": "application"
          }
        }
      },
      {
        "title": "Start a Chat",
        "properties": {
          "lakeburner.initialPrompt.default": {
            "type": "string",
            "default": "",
            "markdownDescription": "Default text seeded into the LakeBurner **Send Initial Prompt** textarea. Use the **Save Default** button in the side panel to update.",
            "scope": "application"
          },
          "lakeburner.singletMode.enabled": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "When `true`, a started chat session ends upon the first task completion. LakeBurner will **not** send the Keep Going nudge-prompt; instead it disables Auto-Run and clears Active Fires once the conversation goes idle.",
            "scope": "application"
          },
          "lakeburner.autoRun.tickIntervalMs": {
            "type": "number",
            "default": 3000,
            "minimum": 0,
            "markdownDescription": "Polling interval (milliseconds) for the Auto-Run ticker. While Auto-Run is ON **and** at least one Active Fire exists, LakeBurner silently attempts **Allow** then **Keep** on this cadence via UIA only (no focus stealing, no mouse movement). **Default `3000`**.",
            "scope": "application"
          },
          "lakeburner.autoRun.requireWindowFocus": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "When `true`, the Auto-Run ticker only fires while the VS Code window is focused. Default `false` — UIA `Invoke()` presses buttons in-place without raising the window or moving the mouse, so background ticks are safe while you work in another app. Set to `true` to opt back in to focus-gating.",
            "scope": "application"
          },
          "lakeburner.autoRun.keepGoingEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "When `true`, the Auto-Run ticker dispatches a **continue** prompt to the configured chat target after `autoRun.keepGoingAfterIdleMs` of no Allow/Keep activity. Useful for nudging a finished assistant turn back into action without manual input.",
            "scope": "application"
          },
          "lakeburner.autoRun.keepGoingAfterIdleMs": {
            "type": "number",
            "default": 15000,
            "minimum": 0,
            "markdownDescription": "How long the chat must be **provably idle** (no Stop/Cancel button visible in the chat input) before dispatching the **Keep Going** prompt. The same value is used as a cooldown between dispatches. The ticker NEVER queues on top of an active generation — if the assistant is still streaming the timer waits indefinitely. Set to `0` to disable (equivalent to `keepGoingEnabled = false`).",
            "scope": "application"
          },
          "lakeburner.autoRun.keepGoingPrompt": {
            "type": "string",
            "default": "Keep going. I trust your intuitions.\n\nIf the task is complete, find ways to improve what we've done in either quantity or quality. Our goal is endless generation with asymtotal diminishing returns. This verifies we reach a state where 'there is no more to be added' and 'the data quality cannot reliably through a variety of sources contemporary to the current year as it is in a state of maximum trustoworthiness'",
            "markdownDescription": "Text dispatched to the active chat target when the Auto-Run ticker decides the conversation has stalled.",
            "scope": "application"
          },
          "lakeburner.autoRun.keepGoingIdleStreak": {
            "type": "number",
            "default": 3,
            "minimum": 1,
            "markdownDescription": "How many consecutive ticker scans must show **no Allow/Keep button AND no chat Stop/Cancel button** before the **Keep Going** prompt is dispatched. Higher values reduce the chance of queueing a prompt while the assistant is still streaming.",
            "scope": "application"
          },
          "lakeburner.autoRun.clearQueuedEnabled": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Redundancy layer. When `true`, each Auto-Run tick also scans for the chat composer's **Remove All Queued** action and invokes it if present. The option only appears when prompts are queued behind an active turn, so on a healthy run this is a silent no-op — it exists to catch the rare case where a Nudge-Prompt slipped past the busy detector.",
            "scope": "application"
          },
          "lakeburner.uia.chatInputNames": {
            "type": "array",
            "items": { "type": "string" },
            "default": [],
            "markdownDescription": "Additional accessible names to try when locating the chat composer's text input for the **Keep Going** nudge. Merged with the built-in defaults (`Ask Copilot`, `Type your task`, `Chat input`, etc.). Only needed if your chat extension uses a non-standard label.",
            "scope": "application"
          },
          "lakeburner.uia.chatSendNames": {
            "type": "array",
            "items": { "type": "string" },
            "default": [],
            "markdownDescription": "Additional accessible names to try when locating the chat composer's **Send** button. Merged with the built-in defaults (`Send`, `Send (Enter)`, `Submit`, etc.).",
            "scope": "application"
          },
          "lakeburner.uia.restoreMinimizedForNudge": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "When `true`, the **Keep Going** nudge calls `ShowWindowAsync(SW_SHOWNOACTIVATE)` on minimized VS Code windows so the chat composer's controls are realized in the UI Automation tree. This does **not** raise the window above other apps, steal keyboard focus, or move the mouse cursor — fullscreen games keep input. Set to `false` to silently skip the nudge while VS Code is minimized."
          },
          "lakeburner.autoRun.preserveForeground": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "When `true`, LakeBurner captures the currently-focused window **before** any dispatch (Start a Chat, Keep Going nudge) and restores it **after**, using the Windows AttachThreadInput trick to bypass the foreground lock. Lets you keep gaming / browsing while the auto-runner does its work — if VS Code does need to surface momentarily, focus returns to your previous app within ~100ms. Windows-only; no-op elsewhere."
          }
        }
      }
    ]
  },
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "compile": "node node_modules/typescript/bin/tsc -p .",
    "build-webview": "node node_scripts/build-webview.mjs",
    "copy-static": "node node_scripts/dist-copy.js",
    "build": "npm run clean && npm run compile && npm run build-webview && npm run copy-static",
    "vscode:prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.19.3",
    "@types/vscode": "^1.118.0",
    "esbuild": "^0.25.12",
    "typescript": "^6.0.3"
  }
}
